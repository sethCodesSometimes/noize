[package]
name = "noize"
version = "0.1.0"
edition = "2024"

[dependencies]
mpv = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
nix = { version = "0.29", features = ["term", "poll"] }
libc = "0.2"
